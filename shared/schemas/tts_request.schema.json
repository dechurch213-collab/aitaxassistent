{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TtsSynthesizeRequest",
  "description": "C7: Запрос orchestrator -> tts POST /synthesize (chunked PCM-ответ)",
  "type": "object",
  "required": ["session_id", "text", "lang"],
  "properties": {
    "session_id": {"type": "string"},
    "text": {"type": "string", "minLength": 1},
    "lang": {"type": "string", "enum": ["ru", "kk"]},
    "voice_id": {"type": "string", "description": "Опционально; по умолчанию primary для lang из tts.yaml"},
    "speed": {"type": "number", "minimum": 0.5, "maximum": 2.0, "default": 1.0}
  }
}
